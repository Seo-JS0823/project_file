SELECT 
    M.NAME AS MANAGER_NAME,
    E.EMPLOYEE_ID,
    E.NAME AS EMP_NAME,
    D.DEPARTMENT_NAME
FROM PROJECT1.DEPARTMENTS D
JOIN PROJECT1.EMPLOYEES M 
  ON D.MANAGER_ID = M.EMPLOYEE_ID
JOIN PROJECT1.EMPLOYEES E 
  ON D.DEPARTMENT_ID = E.DEPARTMENT_ID
ORDER BY M.NAME, E.NAME;